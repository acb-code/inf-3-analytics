# API backend URL
NEXT_PUBLIC_INF3_API_BASE=http://localhost:8000
# Optional: disable SSE (use polling) for unstable tunnels/proxies
# NEXT_PUBLIC_DISABLE_SSE=true
# Optional: reduce status polling frequency (ms)
# NEXT_PUBLIC_POLL_INTERVAL_MS=5000
